# simulator

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run index.ts
```

Este proyecto es un simulador de data de server que publica datos en un broker mqtt para su consumo
